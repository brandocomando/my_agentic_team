{
  "name": "survey-copilot-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Local-first Chrome extension that suggests survey screener answers and fills only after user confirmation.",
  "type": "commonjs",
  "scripts": {
    "llm:serve": "node server/llm-helper.js",
    "test": "node --test tests/*.test.js",
    "validate": "npm test"
  }
}
